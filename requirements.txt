# Core
torch>=2.4
torchvision>=0.19
numpy>=1.26
Pillow>=10.0
PyYAML>=6.0
tqdm>=4.66

# Losses
pytorch-msssim>=1.0.0     # differentiable SSIM for the loss variant

# Metrics (evaluation only -- never hand-rolled, see evaluation/README.md)
lpips>=0.1.4              # LPIPS, AlexNet backbone
pyiqa>=0.1.10             # NIQE -- the only maintained Python implementation that
                          # ships the pristine-model parameters NIQE requires
piq>=0.8.0                # BRISQUE, used as the no-reference fallback if pyiqa
                          # is unavailable (see evaluation/metrics.py)
scikit-image>=0.22        # reference SSIM/PSNR cross-check
scipy>=1.11

# Reporting
matplotlib>=3.8
